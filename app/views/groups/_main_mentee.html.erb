<h1><a><%=@group.name%></a></h1>
<p>Direct message your circle below. Messages will be sent to each user via their selected technology accessibility level.</p>

<% @group.users.where.not(id: current_user).each do |user| %>

<div class="col-sm-4">
    <img class="img-circle img-responsive img-center" src="images/image<%=user.id%>.jpg" alt="">
    <h2><%= link_to user.name, user %></h2>
    <input type="text" class="form-control">
    <button class= "btn btn-info">Send</button>
</div>

<%end%>
